# JWT auth_token 嵌入示例文件
#
# 使用步骤：
# 1. 复制本文件为 deploy/grafana/jwt-secret，写入一个随机字符串（HS256 secret）：
#      openssl rand -hex 32 > deploy/grafana/jwt-secret
# 2. 在平台「可观测性 → 可视化 → 仪表盘 → 设置」的 JWT Secret 字段
#    填入相同内容（GrafanaSetting.jwt_secret）
# 3. 平台打开看板时会签发 60 秒短时 JWT，通过 iframe URL 的
#    ?auth_token= 参数自动登录 Grafana（[auth.jwt] url_login）
#
# 注意：
# - 本文件（jwt-secret）与 jwt-secret.example 不要提交到版本库
# - 两端 secret 必须完全一致，否则 Grafana 拒绝 Token
