# Grafana 看板 provisioning — 演示看板自动加载
apiVersion: 1

providers:
  - name: sxdevops-demo-dashboards
    orgId: 1
    folder: SxDevOps 演示
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
